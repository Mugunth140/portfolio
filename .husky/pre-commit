# Format code
echo "✨ Formatting..."
bunx prettier --write .

# Staging formatted code
echo "🗂️ Staging formatted code..."
git add .

# Run tests only if they exist
echo "Testing..."
if ls **/*.{test,spec}.{js,ts} >/dev/null 2>&1; then
  bun test || exit 1
fi
